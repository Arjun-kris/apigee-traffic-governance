# Copy to .env and edit locally. Do NOT commit .env.
ORG=your-apigee-org
ENV=dev
SHAREDFLOW=global-gateway-governance
KVM_NAME=GLOBAL_GATEWAY_GOVERNANCE
CONFIG_FILE=config/governance-config.example.json

# Optional smoke test URL for an existing API proxy in the environment.
# TEST_URL=https://api.example.com/path
